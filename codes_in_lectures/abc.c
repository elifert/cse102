#include <stdio.h>

int main() {
    int a = 2;
    int b = 5;
    float x = 3.3;
    float result;
    result = (float)a*b + x;
    printf("%05d", b);

}
/* unary operators can be usded with only one term */
/*  quiz question about conversions */